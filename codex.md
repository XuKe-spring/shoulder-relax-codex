## 角色

你是一个前端全栈工程师，擅长 React + TypeScript + Vite 开发。

## 任务

开发一个叫 shoulder-relax 肩颈放松的 Web 应用，帮助办公族在电脑前站起来，用摄像头跟着动画做肩颈放松训练，傻子也能练对。

**核心体验闭环**：用户在 Chrome 浏览器打开网页 → 设置定时提醒间隔 → 关闭页面继续办公 → 到时间浏览器弹出通知 → 点击通知回到网页 → 选择课程 → 站起来开启摄像头 → 跟着动画做动作 → 实时看到自己姿势对比 → 训练结束查看评分。

## 功能模块

### 1. 定时提醒
- Service Worker + Notification API
- 用户可设置间隔（30/45/60/90/120 分钟），可开关
- 通知弹出后点击进入首页

### 2. 训练课程（3 套）
- 微休息（2 分钟，4 动作）、标准课程（5 分钟，8 动作）、深度放松（10 分钟，12 动作）
- 课程数据配置在 `src/data/courses.ts`
- 每个动作：名称、持续时间、TTS 播报文本、SVG 动画类型

### 3. 训练界面（核心，全屏暗色主题）
- 左侧：SVG 人体动画演示标准动作
- 右侧：摄像头镜像 + MediaPipe 骨骼关键点叠加
- 底部状态栏：当前姿势状态（良好/偏差/需要调整）和具体提示
- 顶部：课程名、当前动作名、倒计时
- 底部控制：上一个/暂停继续/下一个/退出
- 语音播报：浏览器 Web Speech API 朗读动作指令

### 4. 姿态检测（仅训练期间开启）
- MediaPipe Pose 从 CDN 加载，33 个关键点
- 训练开始前 3 秒站直校准，采集用户自然站姿基准
- 检测 3 项：耸肩（肩 Y 偏高）、驼背（肩相对髋前移）、头前倾（鼻相对肩前移）
- 每 2 秒采样一次，0.8 秒更新反馈文字
- 所有计算纯本地，摄像头图像不上传

### 5. 训练完成页
- 课程名、完成时间、实际耗时
- 姿势评分（0-100 分，基础 100，每次偏差扣 2 分）
- 偏差汇总（哪类问题出现了几次 + 改进建议）
- 连续打卡天数
- 按钮：再来一次 / 查看历史

### 6. 历史记录
- 按时间倒序展示训练记录
- 统计概览：本周/本月次数、累计分钟、连续打卡天数
- 打卡日历（近 4 周热力图）
- 近 7 天柱状图

### 7. 新手引导
- 首次访问自动进入（localStorage 标记）
- 4 步：欢迎 → 摄像头权限请求 → 提醒间隔设置 → 快速体验
- 每步可跳过

### 8. 设置页
- 提醒间隔、提醒开关、语音播报开关

## 技术栈

- 框架：React 18 + TypeScript + Vite
- 样式：Tailwind CSS v3，暗色主题
- 路由：React Router v6
- 动画：Framer Motion（SVG 动作演示动画）
- 姿态检测：MediaPipe Pose（CDN 加载：`https://cdn.jsdelivr.net/npm/@mediapipe/pose@0.5.1675469404`）
- 语音：Web Speech API（浏览器内置）
- 通知：Service Worker + Notification API
- 存储：localStorage
- 部署：Vercel（免费、自动 HTTPS）

## 要求

1. 必须生成完整可运行的代码，每个文件完成后检查是否能通过 TypeScript 类型检查和 Vite 构建
2. 开发前先通过 Context7 查询 Vite、Tailwind CSS、Framer Motion、MediaPipe Pose 的最新用法
3. 姿态检测逻辑要参考 MediaPipe Pose 官方文档，确保关键点索引和阈值正确
4. 所有数据纯本地，不依赖任何后端服务
5. 功能优先于设计，MVP 先跑通核心流程，样式后续统一调整
